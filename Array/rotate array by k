class Solution {
        public void rotate(int[] nums, int k) {
            int n = nums.length;
            int[] b = new int[n];
            int j=0;
            k=k%n;
            for(int i=n-k;i<n;i++)
            {
                b[j]=nums[i];
                j++;
            }
            for(int i=0;i<n-k;i++)
            {
                b[j] = nums[i];
                j++;
            }
            for(int i=0;i<n;i++)
            {
                nums[i] = b[i];
            }
       }
}
